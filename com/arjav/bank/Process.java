package com.arjav.bank;

// enum constants to check for the keep track of  processes
public enum Process {
	sendMoney, createAccount, changeName, changePIN, displayBalance, deposit, withdraw, closeAccount;
}
